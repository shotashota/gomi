# Action Cable uses Redis by default to administer connections, channels, and sending/receiving messages over the WebSocket.
production:
  adapter: redis
  url: redis://h:p8rlsr0qhhhcl02qb414fu4gh1l@ec2-107-22-209-183.compute-1.amazonaws.com:15399

development:
  adapter: redis
  url: redis://localhost:6379/1

test:
  adapter: async
